import 'package:flutter/material.dart';

class SectionSearchViewBody extends StatelessWidget {
  const SectionSearchViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
