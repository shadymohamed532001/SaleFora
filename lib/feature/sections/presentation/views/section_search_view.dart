import 'package:flutter/material.dart';

import '../widgets/section_search_view_body.dart';

class SectionSearchView extends StatelessWidget {
  const SectionSearchView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SectionSearchViewBody(),
    );
  }
}
